machine:

  node:
    version: 6.3.0


dependencies:
  override:
    - cd .. && git clone git@github.com:quantmind/d3-view.git && npm install
    - cd .. && git clone git@github.com:quantmind/d3-canvas-transition.git && npm install
    - npm install


test:
  override:
    - npm test
    - npm run-script build
    - mv coverage/P* $CIRCLE_ARTIFACTS/coverage
